import type { NextPage, GetStaticProps } from 'next';
import Link from 'next/link';
import Head from 'next/head';
import styles from '/styles/Home.module.css';
import faker from 'faker';

type WelcomeProps2 = {
  rng: number;
  name: string;
  quoteOfDay: string;
};

const Welcome: NextPage<WelcomeProps2> = (props) => {
  return (
    <div className={styles.container}>
      <Head>
        <title>Welcome2</title>
        <meta name='description' content='Generated by create next app' />
        <link rel='icon' href='/favicon.ico' />
      </Head>

      <main className={styles.main}>
        <h1 className={styles.title}>
          Welcome {props.rng} {props.name}
        </h1>

        <p className={styles.description}>{props.quoteOfDay}</p>
        <Link href='/'>
          <a>HOME</a>
        </Link>
      </main>
    </div>
  );
};

export const getStaticProps: GetStaticProps<WelcomeProps2> = (context) => {
  //await new Promise((resolve) => setTimeout(resolve, 1000));

  const result: WelcomeProps2 = {
    rng: faker.datatype.number(100),
    name: `${faker.name.firstName()} ${faker.name.lastName()}`,
    quoteOfDay: faker.lorem.paragraphs(1),
  };

  console.log(`result2`, result);

  return {
    props: { ...result },
    revalidate: 1,
  };
};

export default Welcome;
